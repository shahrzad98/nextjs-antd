import * as React from 'react';
import CardLeague from "../../components/card";

const League = (props) => {
    return (
        <div>
            <CardLeague/>
        </div>
    );
};

export default League